var assert = require("assert")
var game = require("../listener_game")

describe("JSON", function() {
   describe(".parse()", function() {
       it("should detect malformed JSON strings", function(){
           //Test Goes Here
       });
   });

   describe(".parse()", function() {
       it("should detect malformed JSON strings 2", function(){
           //Test Goes Here
       });
   });

   describe(".parse()", function() {
       it("should detect malformed JSON strings 3", function(){
           //Test Goes Here
       });
   });

   describe(".parse()", function() {
       it("should detect malformed JSON strings 4", function(){
           //Test Goes Here
       });
   });

   describe(".parse()", function() {
       it("should detect malformed JSON strings 5", function(){
           //Test Goes Here
       });
   });

  describe('#indexOf()', function(){
    it('should return -1 when the value is not present', function(){
      assert.equal(-1, [1,2,3].indexOf(5));
      assert.equal(-1, [1,2,3].indexOf(0));
    })
  })
});